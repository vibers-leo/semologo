import Link from "next/link";
import Header from "@/components/Header";
import Footer from "@/components/Footer";
import { BLOG_POSTS } from "@/lib/blog";
export const metadata = { title: "로고 이야기와 사용법 | 세모로고", description: "SVG·PNG 로고 사용법, Illustrator 안내, 브랜드 로고 검색 팁을 정리해요." };
export default function BlogPage() { const categories = [...new Set(BLOG_POSTS.map(post => post.category))]; return <><Header/><main className="mx-auto max-w-4xl px-5 py-12 pb-28"><p className="text-sm font-semibold text-indigo-600">SemoLogo Blog</p><h1 className="mt-3 text-3xl font-black">로고를 더 잘 쓰는 이야기</h1><p className="mt-3 text-gray-500">로고 검색부터 SVG·PNG 활용까지, 작업에 바로 쓰는 정보를 정기적으로 전해요.</p><nav aria-label="블로그 카테고리" className="mt-7 flex flex-wrap gap-2">{categories.map(category => <span key={category} className="rounded-full border px-3 py-1.5 text-xs text-gray-600">{category}</span>)}</nav><div className="mt-8 grid gap-4 sm:grid-cols-3">{BLOG_POSTS.map(post => <article key={post.slug} className="rounded-2xl border p-5"><p className="text-xs text-gray-500">{post.category} · {post.date} · {post.readingMinutes}분</p><h2 className="mt-3 text-lg font-bold leading-7">{post.title}</h2><p className="mt-2 text-sm leading-6 text-gray-600">{post.description}</p><div className="mt-3 flex flex-wrap gap-1.5">{post.tags.map(tag => <span key={tag} className="text-xs text-gray-400">#{tag}</span>)}</div><Link className="mt-5 inline-block text-sm font-semibold underline" href={`/blog/${post.slug}/`}>읽어보기 →</Link></article>)}</div></main><Footer/></>; }
