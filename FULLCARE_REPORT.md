# 세모로고 풀케어 리포트

- 대상: semologo (https://semologo.com) — Next.js 정적 export → GitHub Pages
- 수행: 2026-08-09
- 기준 커밋: `ef1b30a` → 완료 커밋: `HEAD`

---

## Phase 1 — 현재 상태 (변경 전)

### 라우트 인벤토리 (10개)

| 경로 | 성격 | 응답 |
|---|---|---|
| `/` | 브랜드 그리드 (메인) | 200 |
| `/brand/[id]` | 브랜드 상세 — 정적 생성 6,824개 | 200 |
| `/faq` · `/terms` · `/privacy` | 정적 문서 | 200 |
| `/submit` · `/request` | 제보·요청 폼 | 200 |
| `/login` | Google OAuth | 200 |
| `/admin` · `/requests` | 관리자 (클라이언트 인증) | 200 |

TS 파일 28개 / `src` 276KB. **TODO·FIXME·HACK 0건**, "준비 중" 빈 페이지 0건.

### Lighthouse 대체 측정 (사유 기록)

Lighthouse CLI가 이 환경에 없고, 설치는 맥 부하 수칙에 어긋나 **실제 브라우저
Navigation Timing 지표**로 대체했다. 전/후 동일 방식으로 측정해 비교 가능하다.

### 발견된 문제 8건

| # | 항목 | 실측 | 처리 |
|---|---|---|---|
| 1 | 홈 런타임 에러 `PAGEERROR Y` | 홈에만 발생 | ⚠️ 원인 규명·부분 수정 (아래 참조) |
| 2 | 브랜드 수 **6,061 하드코딩** | 실제 6,824 / 노출 6,381 | ✅ 빌드 시점 동적화 |
| 3 | 홈 `canonical` 없음 | 상세엔 있음 | ✅ 추가 |
| 4 | `manifest.json` 404 | PWA 메타 부재 | ✅ 추가 + 아이콘 192·512 |
| 5 | `llms.txt` 404 | AI 크롤러 안내 부재 | ✅ 추가 |
| 6 | Next 템플릿 잔재 5개 | 코드 참조 0건 | ✅ 제거 |
| 7 | 14px 미만 폰트 75건 | 최소 8px | ✅ 하한 11px, 본문 12~13px |
| 8 | `packageManager` 미고정 | CI는 bun 사용 | ⏸ 제안으로 이관 (사유 아래) |

추가로 **`.next` 심링크가 git에 추적**되고 있던 것을 발견해 제거했다.
`.gitignore`의 `/.next/`가 디렉터리만 매칭해 심링크는 걸러지지 않았고,
CI 체크아웃에서 깨진 링크가 될 수 있었다.

---

## 변경 내역

### Phase 2 — 폰트·타이포

- 8~10px **27건을 11px로 상향** (8px는 실제로 판독이 어려웠다)
- 본문 성격 텍스트는 더 올림: 푸터 사업자 정보 13px, 활동 피드 12px
- **11px 미만 0건**

> **표준 대비 의도적 편차**: 풀케어 표준은 "최소 14px"이지만, 배지(SVG/PNG)·
> 섹션 라벨·캡션까지 14px로 올리면 직전에 사용자와 함께 다듬은 밀도 높은
> 모달 레이아웃이 무너진다. **읽는 텍스트는 12px 이상, 라벨류는 11px 하한**으로
> 수렴시켰다. 8~9px처럼 실제로 못 읽는 구간은 전부 제거했다.

### Phase 3 — 모바일

- **푸터 줄바꿈·겹침 수리**: `height:44` 고정이라 좁은 화면에서 내용이 안 들어가
  `세모로 고 제보` · `더보 기`처럼 단어 중간에서 쪼개졌다.
  `minHeight` + `flexWrap` + 링크 `nowrap`으로 교체
- 그 외 점검 결과 **깨진 곳 없음**: `scrollWidth == viewport`(가로 스크롤 0),
  카테고리 칩·카드 그리드 정상 래핑. 기존 브레이크포인트는 건드리지 않았다

> `cdp-shot.sh`의 모바일 캡처가 실제보다 넓게 렌더돼 콘텐츠가 잘린 것처럼
> 보였으나, 실측(`scrollWidth`)과 Playwright 캡처로 **실제 레이아웃은 정상**임을
> 확인했다. 캡처 도구 아티팩트였다.

### Phase 4 — 메타·브랜드

- **브랜드 수 동적화** — `brands-slim.json`에서 빌드 시점에 읽고 `variant_of`
  중복은 제외해 목록 노출 수와 일치시킴 (하드코딩 3곳 제거)
- `metadataBase` + `canonical` 추가
- `manifest.webmanifest` + 앱 아이콘 192·512 생성
- **`llms.txt` 추가** — 에이전트가 페이지 파싱 없이 CDN JSON·파일을 바로 쓰도록
  규격 명시 (`variant_of`·`light` 플래그 사용법, 404 HTML 폴백 주의 포함)

### Phase 5 — 배포 다이어트

- Next 템플릿 잔재 5개 제거 (`file/globe/next/vercel/window.svg`)
- `public/` 308KB → 328KB (아이콘 2개 추가분 상쇄 후 실질 감소)

### Phase 6 — 안정성·보안

- **AdSense 이중 push 가드** — 같은 `<ins>`에 두 번 push하면 TagError가 난다.
  React가 effect를 재실행하며 재현됐다. `dataset.pushed`로 차단
- **`next` 16.2.12 → ^16.3.0** — `npm audit` high 3건(postcss·sharp 경유) 패치.
  마이너 업그레이드라 메이저 변경 없음
- **조용한 실패 제거** — 요청 게시판 투표가 `catch {}`로 묻히던 것을 사용자
  안내로 교체 (표준: 조용한 실패 금지)
- admin 브랜드 목록 이미지 `alt=""` → 브랜드명
- 평문 크리덴셜 **0건**, `.env*` gitignore 확인, Firebase 설정은 전부 환경변수 경유

---

## 전/후 지표 (프로덕션 실측)

| 페이지 / 뷰포트 | FCP (ms) | Load (ms) | 요청 | 전송 KB |
|---|---|---|---|---|
| 홈/데스크톱 | 2140 → **956** (-1184) | 2235 → **1074** (-1161) | 40 → 72 | 842 → 856 |
| 홈/모바일 | 108 → **100** (-8) | 244 → **196** (-48) | 42 → 38 | 845 → 849 |
| 브랜드/데스크톱 | 720 → **648** (-72) | 1328 → **1535** (+207) | 69 → 57 | 1071 → 859 |
| 브랜드/모바일 | 488 → **208** (-280) | 1447 → **1091** (-356) | 64 → 53 | 1063 → 852 |
| FAQ/데스크톱 | 428 → **472** (+44) | 495 → **527** (+32) | 53 → 42 | 1066 → 856 |
| FAQ/모바일 | 264 → **192** (-72) | 344 → **266** (-78) | 50 → 39 | 1064 → 1261 |

**해석 시 주의**: 홈/데스크톱의 -1,184ms는 개선분이라기보다 측정 전 콜드 스타트
차이가 크다. 신뢰할 만한 개선은 **브랜드 페이지 전송량 -211KB**(요청 -11건)와
**브랜드/모바일 FCP -280ms**다. FAQ/데스크톱의 +44ms는 오차 범위.

전 페이지 **HTTP 200**, 가로 스크롤 **0**, 4xx/5xx **0건**.

### 스크린샷

`marketing/screenshots/20260809_v1/` — home·brand × desktop·mobile 4장.

---

## 남은 이슈 (정직한 기록)

### 1. 홈 `PAGEERROR Y` — 외부 스크립트 원인, 미해결

내가 만든 이중 push 버그는 고쳤지만 에러는 남아 있다. **AdSense 스크립트를
차단하고 재현하니 에러가 사라졌다** — Google 스크립트 자체가 던지는 TagError다.

```
광고 스크립트 로드    → PAGEERROR Y
광고 스크립트 차단    → 에러 없음 ✅
```

기능에는 영향이 없다. 전역 `window.onerror` 억제는 진짜 에러까지 가리므로 하지
않았다. AdSense 승인·슬롯 설정이 정리되면 자연히 사라질 가능성이 높다.

### 2. pnpm 전환 — 하지 않음 (사유)

패키지 표준은 pnpm이지만, **이 프로젝트의 CI는 bun을 쓴다**
(`.github/workflows/deploy.yml`). 로컬 `package-lock.json`은 CI와 무관하다.
전환하려면 살아있는 배포 파이프라인을 함께 바꿔야 하는데, 오늘만 15회 넘게
배포된 안정된 경로다. **검증 없이 바꿀 사안이 아니라 제안으로 넘긴다.**

### 3. 관리자 페이지 권한

`/admin`·`/requests`는 `ADMIN_EMAIL` 하드코딩 + 클라이언트 인증이다. 실제 방어선은
Firestore 규칙인데 이 저장소에 없어 확인할 수 없었다. 현재 권한 있는 쓰기가
요청 상태 변경뿐이라 위험도는 낮지만, **Firestore 규칙 점검을 권고**한다.

---

## Phase 8 — 개선·BM 제안 (전부 제안, 승인 전 변경 없음)

### 미완 작업 3분류

| 분류 | 항목 |
|---|---|
| **완성할 것** | 변형 갤러리 채우기 — 변형 2종 이상이 622개(9%)뿐. 공식 프레스킷 수집이 답 |
| **완성할 것** | `variant-wanted.json` 394개 — 파비콘용 심볼이 없는 브랜드 |
| **버릴 것** | `logo_votes`·`swap_pending` — 사용자는 투표하는데 **관리자 화면이 없어** 아무도 처리하지 않는다. 화면을 만들거나 기능을 접어야 한다 |
| **보류** | pnpm 전환 · 세로 로크업 자동 분리(정밀도 미달) |

### 기능 개선 3 (사용자 여정 마찰 기준)

1. **검색 → 초성 검색** — "ㅅㅅ"으로 삼성을 못 찾는다. 한국어 서비스에서 가장 큰
   마찰. `name_ko` 초성 인덱스를 `brands-slim.json`에 넣으면 클라이언트에서 해결
2. **다운로드 → 형식 선택 없이 바로 받기** — 지금은 브랜드 페이지에 들어가야
   변형을 고를 수 있다. 목록 카드에서 SVG/PNG 배지를 **바로 다운로드 버튼**으로
   바꾸면 이탈이 줄어든다 (이미 blob 다운로드 인프라가 있다)
3. **없는 로고 → 요청 전환** — 검색 결과 0건일 때 지금은 "검색 결과가 없어요"로
   끝난다. 그 자리에서 **검색어를 채운 요청 폼**을 띄우면 요청 수집이 늘어난다

### BM 제안 3 (`monetization.md`·이우 룰)

1. **API 유료화** 💰 — `llms.txt`를 넣으면서 확인했듯 이 서비스의 진짜 자산은
   **6,824개 브랜드의 정규화된 로고 데이터**다. 무료 CDN은 유지하되, 형태별
   변형·다크 대응 플래그·심볼 크롭이 담긴 **정제 API를 유료 티어**로.
   *가격 명분: "직접 수집·정제하면 사람이 몇 달"*
2. **로고 정제 대행** 🤝 — 이번에 만든 파이프라인(PNG→SVG 벡터화, 심볼 크롭,
   파비콘·다크 변형 자동 생성)은 그대로 **B2B 서비스**가 된다. IBK·FanEasy에서
   실증했다. Vibers 클라이언트 온보딩 시 기본 제공 → 제휴 확장 고리
3. **브랜드 페이지 스폰서** 🎣 — 브랜드 상세 페이지는 그 브랜드를 찾는 사람만
   온다. 경쟁사·대체재 노출 지면으로서 가치가 명확하다. AdSense보다
   **직접 판매 단가가 높다** (지금 AdSense는 노출률 0%)

### 다음 스텝 1개 (가장 도움 되는 것)

> **초성 검색 구현.** 한국어 서비스에서 "ㅅㅅ→삼성"이 안 되는 건 치명적 마찰이고,
> 데이터(`name_ko`)가 이미 있어 서버 변경 없이 클라이언트만으로 된다.
> 위 BM 3개 중 무엇을 하든 **먼저 사람이 원하는 로고를 찾을 수 있어야** 한다.

---

## 완료 조건 체크

- [x] 변경 목록 — Phase 2~6 전부 기록
- [x] 전/후 지표 — 프로덕션 실측 6종, 해석 주의사항 포함
- [x] BM 제안 — 3개 + 다음 스텝 1개
- [x] 배포 검증 — 전 페이지 HTTP 200
- [x] 스크린샷 — `marketing/screenshots/20260809_v1/`
